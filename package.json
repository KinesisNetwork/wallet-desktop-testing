{
  "name": "kinesis-wallet-ui",
  "version": "1.0.0",
  "description": "automation tests for the kinesis-wallet-ui",
  "main": "index.js",
  "scripts": {
    "lint": "tslint 'src/**/*.ts'",
    "lint-fix": "tslint 'src/**/*.ts' --fix",
    "compile": "npm run lint && rm -rf dist/* && tsc",
    "test": "cucumber-js src/features/"
  },
  "author": "Kinesis Network",
  "license": "ISC",
  "dependencies": {
    "chai": "^4.1.2",
    "cucumber": "^4.2.1",
    "gemfile": "^1.0.0",
    "tslint": "^5.10.0",
    "typescript": "^2.8.3",
    "webdriverio": "^4.13.1"
  }
}